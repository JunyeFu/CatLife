# BlueLM SDK Library Slot

Put the official vivo BlueLM Android SDK AAR here when it is available:

```text
Assets/Plugins/Android/libs/llm-sdk-release.aar
```

This repository does not include a fake AAR. The stage 2 bridge compiles as a skeleton and reports `SDK_NOT_LINKED` instead of crashing until the real SDK is linked.
