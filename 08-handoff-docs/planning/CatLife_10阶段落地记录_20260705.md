# CatLife 10 阶段落地记录 2026-07-05

目标：按 `CatLife_蓝心端侧接入与隐私行为闭环开发阶段_20260705.md` 的 10 阶段顺序逐步落地。每次只关闭一个明确阶段；10 阶段全部完成后，再按 PPT 要求重新清点缺陷，发现不符合项后继续逐项修复。

## 总体状态

| 阶段 | 名称 | 当前状态 | 证据口径 |
| --- | --- | --- | --- |
| 0 | 基线冻结与差异确认 | 已完成 | 当前 Git、Unity 编译、Console、密钥边界和文档口径已对齐 |
| 1 | BlueLM Unity 端接口骨架 | 已完成 | Unity 端可切换 LLM runtime、BlueLM 请求/回调 DTO、端侧客户端骨架和验证器检查已落地 |
| 2 | BlueLM Android Plugin 骨架 | 已完成 | Android Java 桥接骨架、init/generate 回调、权限入口、默认模型路径和 minSdk 28 已落地 |
| 3 | 端侧 JSON Guard 与 Prompt Builder | 已完成 | Android/Unity 双端 guard、prompt builder、BlueLM request/feedback schema 和验证器检查已落地 |
| 4 | BlueLM generate 闭环 | 已完成 | 反射式 SDK adapter、Android generate 输出 guard、Unity source/fallback 遥测和 Java 编译检查已落地 |
| 5 | 真实 Android 行为事件桥 | 已完成 | BehaviorEvent/Sanitizer、Unity 接收器、Android Java sender、RealtimeFeatureEngine 映射和隐私拒绝验证已落地 |
| 6 | CatBehaviorDriver 消费蓝心建议 | 已完成 | `recommendedLocalAction` 已经通过本地解释器转为安全动作，气泡和遥测消费闭环已落地 |
| 7 | 云端 API 备用路径 | 已完成 | vivo 云端私有 APK fallback 边界、占位 key 拒绝、遥测状态和验证器检查已落地 |
| 8 | Splash 启动页集成 | 已完成 | 开屏素材已进入 Resources，首页运行时 20:9 splash overlay、自动淡出、点击跳过和验证器检查已落地 |
| 9 | vivo 云真机 APK 验收 | 部分落地 | 已生成真实版 APK、构建脚本、SHA256、脱敏私有 key 边界和本机 ADB 可用性证据；仍缺已连接云真机的 install、logcat、录屏和实测 |
| 10 | PPT 与交付材料同步 | 部分落地 | 代码包、PPT、海报和 manifest 已准备；仍缺视频、云真机安装/logcat/录屏证据，阶段 9 未通过前不能关闭 |

## 阶段 0：基线冻结与差异确认

完成时间：2026-07-05。

### 当前 Git 基线

- 分支：`main`
- 阶段 0 开始前 HEAD：`99436a9`
- 开始时跟踪文件差异：仅 `.gitignore` 中新增 Unity 私有 Resources 元数据忽略规则。
- 新增忽略边界：
  - `work/CatLife_Unity_Main/Assets/Resources.meta`
  - `work/CatLife_Unity_Main/Assets/Resources/CatLifePrivate.meta`

### Unity 基线

- Unity 实例：`CatLife_Unity_Main@8ee0a795a101edd4`
- Unity 版本：`6000.4.9f1`
- 初始问题：Unity Console 曾出现 `MockCatLLMClient.cs` 找不到 `VivoCloudDemoConfig` 的编译错误。
- 处理方式：通过 Unity MCP 强制刷新 AssetDatabase 并请求脚本编译。
- 当前结果：Unity Console 错误数为 0。
- 当前仍存在的非阻塞警告：
  - `CatTownWalker.cs` 中 4 个旧回退字段未使用。
  - `CatLifePreviewSceneBuilder.cs` 中 2 个 Unity 过时 API 警告。
  - Unity MCP WebSocket 一条初始化相关警告。

### 当前大模型与隐私边界

- `ICatLLMClient`、`MockCatLLMClient`、`VivoCloudDemoConfig`、`PrivacyGateway`、`CatBehaviorDriver`、`RealtimeFeatureEngine` 当前均存在。
- `MockCatLLMClient` 当前口径：检测到本机私有 vivo 云端配置时优先真实 API；未配置或失败时回退本地 mock。
- 本机私有配置路径：`work/CatLife_Unity_Main/Assets/Resources/CatLifePrivate/vivo_cloud_credentials.json`。
- 该私有配置存在于本机，且被 `.gitignore` 忽略。
- 仓库公开范围检查未发现 vivo AppKEY 特征串。
- 真实版/最终提交 APK 的口径：本机导出时必须把私有 Resources 中的云真机可用 key 打进 APK，方便上传云真机后直接演示真实 API。
- GitHub、代码包、公开示例和提交记录的口径：不得包含真实 AppKEY。

### 阶段 0 结论

阶段 0 可以关闭。当前基线适合继续进入阶段 1，但下一阶段必须保持以下约束：

- 不改动真实 key 的公开边界。
- 不在公开文档中输出完整 key。
- 不把 Unity Console 旧警告误判为阶段 1 的阻塞项。
- Runtime Assembly Validator 菜单项曾被触发，但本轮未取得可检索的 PASS/FAIL 日志；后续阶段如需作为证据，应补充稳定日志或脚本化验证。

## 阶段 0 后的阶段 1 入口（已执行）

阶段 1 只处理 BlueLM Unity 端接口骨架：

- 保留现有 `ICatLLMClient.RequestSuggestion(...)` 对外契约。
- 在不破坏现有 mock 和 vivo 云端 fallback 的前提下，补出 BlueLM 端侧请求/响应 DTO、可替换客户端和清晰日志。
- 阶段 1 验收必须包括 Unity 编译无错误、Console 无新增错误、隐私边界检查通过。

## 阶段 1：BlueLM Unity 端接口骨架

完成时间：2026-07-05。

### 本阶段落地内容

- 新增 `LlmRuntimeMode`：定义 `Auto`、`MockOrVivoCloud`、`BlueLmOnDevice`、`LocalTemplateOnly` 四种 Unity 侧 runtime 模式。
- 新增 `LlmClientFactory : MonoBehaviour, ICatLLMClient`：未来场景可把 `CatBehaviorDriver` 的 LLM 引用切到工厂组件；当前场景仍保留 `MockCatLLMClient`，不破坏已有演示路径。
- 新增 `BlueLmUnityRequest`：统一封装 requestId、超时、system/developer prompt、去标识化用户上下文 JSON 和输出 schema。
- 新增 `BlueLmAndroidEvent`：统一解析 Android 回调成功/失败事件，并把成功输出收敛到 `LLMBehaviorSuggestion`。
- 新增 `BlueLmCallbackReceiver`：提供 `OnBlueLmEvent(string json)` 回调入口，供后续 Android 插件通过 `UnitySendMessage` 回传。
- 新增 `BlueLmOnDeviceClient : MonoBehaviour, ICatLLMClient`：Editor/非 Android 环境自动本地 fallback；Android 环境预留 `AndroidJavaClass` 桥接；超时或桥接失败不阻塞猫咪行为。
- 更新 `CatLifeRuntimeAssemblyValidator`：加入 BlueLM requestId、成功回调 JSON、失败回调 JSON 的可重复检查。

### 保留边界

- 没有引入 Android SDK、AAR、Gradle 或 Manifest 变更；这些属于阶段 2。
- 没有替换当前场景中的 `MockCatLLMClient` 引用；本机私有 vivo 云端配置优先真实 API、失败回退 mock 的路径保持不变。
- LLM 输出仍只能进入 `LLMBehaviorSuggestion` 的高层权重、短句和 `showBubble`，不新增坐标、Transform、NavMesh、Animator 命令入口。
- 真实 AppKEY 仍只允许存在于本机 ignored 私有 Resources；本阶段未把 key 写入仓库。

### 验证记录

- Unity 脚本刷新并编译：Console 错误数为 0。
- 当前仅剩旧警告：`CatTownWalker` 未使用字段、`CatLifePreviewSceneBuilder` 过时 API；阶段 1 未新增 warning。
- Runtime Validator 直接返回：
  - `PASS CatLife runtime assembly validation: scene wiring, NavMesh runtime, cat behavior driver, recognition/LLM systems, BlueLM Unity adapter, config schemas, UI binding, and 11 animator states are present.`
- 公开范围密钥扫描仍需在提交前执行；阶段 1 不应提交真实 key。

## 阶段 2 入口

阶段 2 只处理 BlueLM Android Plugin 骨架：

- 建立 `Assets/Plugins/Android/` 侧 Kotlin/Java 桥接目录和最小插件结构。
- 让 Android 侧可以接收 `BlueLmUnityRequest` JSON，并通过 `UnitySendMessage("CatLifeBlueLmCallbackReceiver", "OnBlueLmEvent", json)` 回传 init/generate 结果。
- 不在阶段 2 声称端侧 SDK 真正可用，除非有云真机或真机 logcat 证据。

## 阶段 2：BlueLM Android Plugin 骨架

完成时间：2026-07-05。

### 本阶段落地内容

- 新增 `Assets/Plugins/Android/AndroidManifest.xml`：
  - 声明 `MANAGE_EXTERNAL_STORAGE` 和 Android 13 前的读取权限边界。
  - 写入默认模型路径 `/sdcard/1225/1.7.0.4_1225_mtk9500`。
- 新增 Android Java 桥接目录：
  - `BlueLmUnityBridge.java`：提供 `init(...)`、`generate(...)`、权限检查和跳转入口。
  - `BlueLmEngine.java`：提供默认模型路径、初始化骨架、生成骨架和明确失败码。
  - `BlueLmUnityCallback.java`：统一 `UnitySendMessage(..., "OnBlueLmEvent", json)` 回传，并写 logcat。
  - `BlueLmPermissionHelper.java`：提供 Android 11+ 所有文件访问权限检查和设置页跳转。
- 新增 `Assets/Plugins/Android/libs/README.md`：说明官方 `llm-sdk-release.aar` 的放置位置；本阶段不伪造 SDK。
- 修改 `ProjectSettings.asset`：`AndroidMinSdkVersion` 从 25 提升到 28；当前 `AndroidTargetArchitectures: 2` 保持 ARM64。
- 更新 `BlueLmOnDeviceClient`：
  - 新增默认模型路径字段。
  - Android runtime 下 Awake 可调用 `BlueLmUnityBridge.init(callbackObject, modelPath)`。
  - generate 仍沿用阶段 1 的 requestId、timeout 和 fallback 机制。
- 更新 `BlueLmCallbackReceiver`：
  - 对 `requestId=bluelm_init` 的初始化事件单独记录日志，不要求 pending request。
- 更新 `BlueLmAndroidEvent`：
  - 增加 `code` 字段，用于承接 Android 初始化和生成失败码。
- 更新 `CatLifeRuntimeAssemblyValidator`：
  - 校验 Android 插件文件、默认模型路径、`UnitySendMessage`、`SDK_NOT_LINKED`、minSdk 28 和 ARM64。

### 保留边界

- 没有接入真实 vivo BlueLM AAR；因此不能宣称“端侧 SDK 已完成”。
- 当前 Java 骨架在模型目录缺失、权限缺失或 SDK 未链接时返回明确失败码，并由 Unity fallback 保证猫咪行为不阻塞。
- 没有写入任何真实 AppKEY；真实版/最终提交 APK 的云端 key 仍只来自本机 ignored 私有 Resources。
- 没有新增 Gradle/Kotlin 插件配置，避免在官方 SDK 未确认前破坏 Unity Android 构建链。

### 验证记录

- 阶段 2 验证入口：`CatLife/Runtime/Validate Runtime Assembly`。
- 本阶段要求 Runtime Validator 返回包含 `Android BlueLM bridge skeleton` 的 PASS。
- 提交前仍需执行 Unity Console 错误检查、公开范围密钥扫描、staged diff 检查。

## 阶段 3 入口

阶段 3 只处理端侧 JSON Guard 与 Prompt Builder：

- 在 Android 侧补 `BlueLmPromptBuilder` 与 `BlueLmJsonGuard`。
- 在 Unity 侧补对应 schema 文件，约束 BlueLM 只能返回严格 JSON。
- 保持“不采集隐私、不读原始输入、不输出坐标/Transform/NavMesh/Animator 命令”的安全边界。

## 阶段 3：端侧 JSON Guard 与 Prompt Builder

完成时间：2026-07-05。

### 本阶段落地内容

- 新增 `Assets/Configs/bluelm_catlife_feedback_schema.json`：
  - 固定 `version=catlife.bluelm.feedback.v1`。
  - 要求 `recommendedLocalAction`、6 个安全布尔位和行为权重字段。
  - 禁止安全布尔位为 true。
- 新增 `Assets/Configs/bluelm_unity_request_schema.json`：
  - 固定 `schemaVersion=catlife.bluelm.request.v1`。
  - 约束 `requestId`、`timeoutMs`、prompt 字段和 `userContextJson`。
  - 明确阻断 `rawText`、`packageName`、`clipboard`、`screenContent`、`x/y`。
- 新增 Android `BlueLmJsonGuard.java`：
  - `validateRequest(...)` 检查 requestId 与 `userContextJson` 隐私禁词。
  - `validateOutput(...)` 检查严格 JSON、版本、安全布尔位和输出禁词。
- 新增 Android `BlueLmPromptBuilder.java`：
  - 将 Unity request 包装为 `catlife.bluelm.prompt.v1` prompt envelope。
  - 标记 `strictJsonOnly=true` 和隐私策略。
- 更新 Android `BlueLmEngine.java`：
  - generate 前先跑 `BlueLmJsonGuard.validateRequest(...)`。
  - SDK 未接入时仍返回明确失败码，但会经过 prompt builder 生成可审计 envelope。
- 更新 Unity `LLMBehaviorSuggestion.cs`：
  - 增加 `version`、`recommendedLocalAction` 和 6 个安全布尔位。
  - 安全布尔位任一为 true 时拒绝输出。
  - 允许 `quiet` mood，并继续 clamp 权重。
- 更新 Unity `PrivacyGateway.cs`：
  - 输入侧补 `rawText`、`packageName`、`clipboard`、`screenContent`、`x/y` 等禁词。
- 更新 Unity `CatPromptBuilder.cs` 和 `CatPromptContext.cs`：
  - prompt/schema 明确 strict JSON、禁止 Markdown/解释、禁止坐标/相机/麦克风/剪贴板/包名等隐私或控制项。
- 更新 `CatLifeRuntimeAssemblyValidator`：
  - 验证两个 schema、Android guard/prompt builder、Unity 严格输出安全位和拒绝逻辑。

### 保留边界

- 本阶段没有接入真实 BlueLM generate；那是阶段 4。
- 本阶段没有构建或上传 APK；那是阶段 9。
- 本阶段没有写入任何真实 AppKEY；私有 key 仍只允许存在于本机 ignored Resources。
- Android guard 只扫描 `userContextJson`，不扫描 schema 字段名本身，避免误拒绝合法请求。
- Android output guard 只扫描 `suggestedLine` 与安全布尔位，避免误拒绝 `coordinateCommandIncluded=false` 这类安全字段名。

### 验证记录

- 阶段 3 验证入口：`CatLife/Runtime/Validate Runtime Assembly`。
- 本阶段要求 Runtime Validator 返回包含 `BlueLM JSON guards` 的 PASS。
- Java 骨架仍需用 Android `android.jar` 加临时 UnityPlayer stub 做 `javac` 编译检查。
- 提交前仍需执行 Unity Console 错误检查、公开范围密钥扫描、staged diff 检查。

## 阶段 4 入口

阶段 4 只处理 BlueLM generate 闭环：

- 在 Android `BlueLmEngine.generateJsonAsync(...)` 中接入真实 SDK generate 调用位置。
- generate 输出先过 Android `BlueLmJsonGuard.validateOutput(...)`，再回传 Unity。
- Unity 收到后继续过 `LLMBehaviorSuggestion.TryBuildSafe(...)`，非法输出 fallback。

## 阶段 4：BlueLM generate 闭环

完成时间：2026-07-05。

### 本阶段落地内容

- 新增 Android `BlueLmSdkAdapter.java`：
  - 通过反射查找 `LlmManager`、`LlmConfig` 和 `TokenCallback`，避免在官方 AAR 包名最终确认前把工程写死。
  - 按蓝心文档设置纯文本推理配置：`modelPath`、`multimodal=false`、`nPredict=256`、`nCtx=2048`、`nThreads=4`、`npuPower=80`、`temperature=0`、`topP=1`、`topK=1`。
  - 支持 `generate(prompt, TokenCallback)` token 累积，完成后回传完整文本。
- 更新 Android `BlueLmEngine.java`：
  - `init(...)` 不再固定返回 `SDK_NOT_LINKED`；权限和模型目录通过后尝试 SDK adapter init。
  - `generateJsonAsync(...)` 现在会构造 BlueLM prompt、调用 adapter generate、抽取首段 JSON、执行 Android 输出 guard，再回传 Unity。
  - 新增 `CODE_GENERATE_FAILED` 和 `CODE_UNSAFE_OUTPUT`，异常或不合规输出不会阻塞猫咪行为。
- 更新 Android `BlueLmJsonGuard.java`：
  - 增加 `extractJson(...)`，支持从模型输出中截取首段 JSON 再做严格校验。
- 更新 Android `BlueLmUnityCallback.java`：
  - generate logcat 改为 `BlueLM generate complete ... payloadBytes=...`。
  - 回调 JSON 增加 `source`，成功为 `bluelm_on_device`，失败为 `local_template`。
- 更新 Unity `BlueLmAndroidEvent.cs`、`BlueLmOnDeviceClient.cs`、`CatBehaviorTelemetry.cs`：
  - Unity 能保存 `LastSource` 和 `LastFailureReason`。
  - 猫咪遥测行增加 `llm_source` 与 `llm_error`，用于 Play Mode 或真机证据判断是否真的走到端侧模型。
- 更新 `CatLifeRuntimeAssemblyValidator.cs`：
  - Runtime Validator 新增 BlueLM generate bridge、SDK adapter、source 字段和 fallback 状态字段检查。

### 保留边界

- 本阶段没有提交真实 vivo AAR；如果 `llm-sdk-release.aar` 尚未放入 `Assets/Plugins/Android/libs/`，云真机会得到明确 `SDK_NOT_LINKED` 或 init 失败码并 fallback。
- 本阶段没有提交真实 AppKEY；真实版/最终提交 APK 仍通过本机 ignored `Assets/Resources/CatLifePrivate/vivo_cloud_credentials.json` 带入云真机可用 key，GitHub 和代码包继续排除明文密钥。
- 本阶段完成的是代码级 generate 闭环，不等同于阶段 9 的云真机真实 SDK 成功验收；真机 logcat 和 screenrecord 仍是阶段 9 的硬证据。

### 验证记录

- `javac -encoding UTF-8 -source 11 -target 11` 加 Android `android.jar` 和临时 `UnityPlayer` stub：通过。
- `git diff --check`：通过，仅有 Windows 换行提示。
- 后续提交前仍需执行 Unity Console 错误检查、Runtime Validator、公开范围密钥扫描和 staged diff 检查。

## 阶段 5 入口

阶段 5 只处理真实 Android 行为事件桥：

- 建立去标识化行为事件 DTO、输入 sanitizer 和 Android 到 Unity 的事件桥。
- 不做 Accessibility，不读取跨 App 内容、不传原始坐标、不采集输入框文本。
- 让 `RealtimeFeatureEngine` 使用真实 App 内事件更新窗口聚合，同时继续保留 mock 作为 Editor 回退。

## 阶段 5：真实 Android 行为事件桥

完成时间：2026-07-05。

### 本阶段落地内容

- 新增 Unity `BehaviorEvent.cs`：
  - 固定 `schemaVersion=catlife.behavior_event.v1`。
  - 只允许事件类型、页面/区域标签、时长和聚合数值，不包含 raw text、坐标、截图、包名、剪贴板。
- 新增 Unity `BehaviorEventSanitizer.cs`：
  - 白名单事件：`UiTap`、`UiButton`、`UiScroll`、`PageEnter`、`PageExit`、`FocusStart`、`FocusCancel`、`FocusComplete`、`Unlock`、`CatTap`、`CatLongPress`、`ScenePointTap`、`AppPause`、`AppResume`。
  - JSON 解析前扫描并拒绝 `rawText`、`raw_input_text`、`x/y`、`packageName`、`clipboard`、`screenContent`、`screenshot`、`ocr`、`preciseLocation` 等字段。
- 新增 Unity `AndroidBehaviorEventBridge.cs`：
  - 自动创建 `CatLifeAndroidBehaviorEventBridge` 接收器。
  - Android 通过 `OnBehaviorEvent(string json)` 回传事件。
  - Unity 本地 UI 可用 `RecordUnityEvent(...)` 记录标准事件标签，但默认不重复写入 feature engine，避免双倍计数。
  - `OnApplicationPause/Focus` 记录 AppPause/AppResume 生命周期事件。
- 更新 `RealtimeFeatureEngine.cs`：
  - 新增 `RecordBehaviorEvent(BehaviorEvent)`，把标准事件映射到现有 tap/page/focus 聚合窗口。
- 更新 `CatLifeHomeUiController.cs` 和 `CatBehaviorDriver.cs`：
  - 首页按钮、页面进入/退出、开始/取消/完成专注、猫咪点击/长按、地面点击、场景点点击都会产生标准事件镜像。
- 新增 Android `com.catlife.recognition.CatLifeEventBridge.java`：
  - 提供 `sendEvent(...)`、`notifyAppPause()`、`notifyAppResume()`。
  - 只构造安全 JSON，并通过 `UnitySendMessage("CatLifeAndroidBehaviorEventBridge", "OnBehaviorEvent", json)` 发给 Unity。
- 更新 `behavior_event_schema.json`：
  - 记录阶段 5 的事件枚举、Unity 接收器、Android sender 和禁用跨 App/Accessibility/原始坐标的策略。
- 更新 `CatLifeRuntimeAssemblyValidator.cs`：
  - 验证桥接文件存在。
  - 实际构造 safe JSON 验证 sanitizer 接收。
  - 实际构造 `rawText` 和 `x/y` payload 验证 sanitizer 拒绝。

### 保留边界

- 本阶段不做 Accessibility，不读取跨 App 内容，不采集输入框原文，不存原始触摸坐标。
- 本阶段没有构建或上传 APK；云真机 logcat 验收仍属于阶段 9。
- 本阶段没有写入任何真实 AppKEY；真实版/最终提交 APK 的私有 key 边界不变。
- Android Java sender 提供生命周期/事件发送能力；Unity 侧 `OnApplicationPause/Focus` 也会记录生命周期事件，因此不需要在本阶段强制替换 Unity 默认 Activity。

### 验证记录

- 阶段 5 验证入口：`CatLife/Runtime/Validate Runtime Assembly`。
- 本阶段要求 Runtime Validator 返回包含 `Android behavior event bridge` 的 PASS。
- Java 骨架仍需用 Android `android.jar` 加临时 UnityPlayer stub 做 `javac` 编译检查。
- 提交前仍需执行 Unity Console 错误检查、公开范围密钥扫描、staged diff 检查。

## 阶段 6 入口

阶段 6 只处理 CatBehaviorDriver 消费蓝心建议：

- 让 `recommendedLocalAction`、`roamWeightBias`、`quietIdleWeightBias`、`socialResponseWeightBias` 和 `showBubble/suggestedLine` 真正进入猫咪行为偏好。
- 保持模型不能直接控制坐标、Transform、NavMesh、Animator 或 Physics。
- Runtime Validator 需要证明模型建议只走高层偏置和安全本地动作类别。

## 阶段 6：CatBehaviorDriver 消费蓝心建议

完成时间：2026-07-05。

### 本阶段落地内容

- 新增 `CatLlmBehaviorInterpreter.cs`：
  - 将 `recommendedLocalAction` 归一化为当前 schema 的 `none`、`quiet_idle`、`soft_roam`、`social_response`。
  - 兼容旧研究文档中的 `quiet_companion`、`gentle_return`、`reward_after_focus`，但进入 Unity 前会分别归一化为 `quiet_idle`、`soft_roam`、`social_response`。
  - 输出只转换为 `CatBehaviorDecision` 的高层状态、优先级、冷却和兴趣点 tag，不产生坐标、Animator state、Transform 或 NavMesh 命令。
- 更新 `LLMBehaviorSuggestion.cs`：
  - `SanitizeRecommendedAction(...)` 现在会把旧别名归一化到当前 schema，避免模型或旧 prompt 返回别名时被静默丢弃。
- 更新 `CatBehaviorDriver.cs`：
  - 蓝心安全输出通过 `ApplySafeLlmSuggestion(...)` 进入本地动作和气泡两条路径。
  - `TryApplyPendingLlmDecision(...)` 在常规 scorer 前消费一次安全本地动作，仍受移动状态、动作 hold、冷却和本地导航规则约束。
  - 增加 `llmLocalActionCooldownSeconds` 和 `llmBubbleCooldownSeconds`，避免模型频繁打断猫咪行为。
  - 气泡只在本地规则允许时调用 `CatBubblePresenter`，专注态默认抑制高打扰气泡。
- 更新 `CatBubblePresenter.cs`：
  - `bluelm_on_device`、`llm_behavior`、`vivo_cloud` 会被识别为智能来源，走更合适的引导语格式。
- 更新 `CatBehaviorTelemetry.cs`：
  - 追加 `llm_action` 和 `llm_bubble`，用于 Play Mode、真机和云真机日志判断“模型建议是否真正被本地消费”。
- 更新 `CatLifeRuntimeAssemblyValidator.cs`：
  - 新增别名归一化、`soft_roam -> Roam`、`quiet_idle -> IdleBreath`、专注态气泡抑制、解释器源码入口、行为驱动入口、气泡来源和遥测字段检查。

### 保留边界

- 模型仍不能直接控制猫咪坐标、Transform、NavMesh、Animator、Collider、Camera 或 Physics。
- 本阶段没有构建或上传 APK；云真机真实调用、logcat 和录屏仍属于阶段 9。
- 真实版/最终提交 APK 必须包含本机 ignored 私有 Resources 中的云真机可用 key；GitHub、代码包、日志、截图、PPT、海报、录屏字幕和公开文档仍不得包含明文 AppKEY。

### 验证记录

- 阶段 6 验证入口：`CatLife/Runtime/Validate Runtime Assembly`。
- 本阶段要求 Runtime Validator 返回包含 `BlueLM local behavior bias/action bridge` 的 PASS。
- 提交前仍需执行 Unity Console 错误检查、公开范围密钥扫描、私有 key ignore 检查和 staged diff 检查。

## 阶段 7 入口

阶段 7 只处理云端 API 备用路径与真实版 APK 的私有 key 边界：

- 比赛长期主线仍优先 BlueLM 端侧；vivo 云端 API 只是开发期、非云真机环境或端侧不可用时的备用演示路径。
- 真实版/最终提交 APK 必须包含本机 ignored 私有 Resources 中的云真机可用 key，以便上传云真机后直接录制真实 API 或明确 fallback。
- GitHub、代码包、公开示例、日志、截图和文档不得包含明文 AppKEY。
- Runtime Validator 需要证明公开示例 key 不会被当作可用凭据，并且私有 Resources 路径被 `.gitignore` 保护。

## 阶段 7：云端 API 备用路径

完成时间：2026-07-05。

### 本阶段落地内容

- 更新 `VivoCloudDemoConfig.cs`：
  - `HasUsableCloudCredentials` 现在会拒绝 `DO_NOT_COMMIT_REAL_APP_KEY`、`REPLACE_WITH_LOCAL_PRIVATE_KEY`、`YOUR_APP_KEY` 等占位 key。
  - 云端 endpoint 必须是 HTTPS。
  - 新增 `RedactedAppId`，日志和遥测只能显示脱敏 AppID。
- 更新 `MockCatLLMClient.cs`：
  - 保留现有短期路径：检测到本机私有 `Resources/CatLifePrivate/vivo_cloud_credentials.json` 时优先请求 vivo 云端，失败回退本地模板。
  - 新增 `LastSource`、`LastFailureReason`、`LastCloudRequestId`、`LastCloudStatusCode`、`LastCloudAppIdRedacted`、`LastCloudConfigUsable`。
  - 云端请求失败时记录失败原因，但不记录 Authorization header 或 AppKEY。
- 更新 `CatBehaviorTelemetry.cs`：
  - 当 LLM 组件是 `MockCatLLMClient` 时，遥测行输出 `llm_source`、`llm_error`、`vivo_app`、`vivo_status`。
  - 云真机或 Play Mode 中至少可以判断当前是 `vivo_cloud`、`local_template`、`vivo_cloud_pending` 还是失败 fallback。
- 更新 `CatLifeRuntimeAssemblyValidator.cs`：
  - 新增 `ValidateVivoCloudFallback(...)`。
  - 验证占位 key 不可用、非 HTTPS endpoint 不可用、公开示例文件仍是占位配置、`.gitignore` 保护私有 Resources、遥测包含 vivo 云端状态字段。

### 保留边界

- 本阶段不把真实 AppKEY 写进 Git。
- 本阶段不构建 APK；阶段 9 仍需要云真机安装、logcat 和录屏证据。
- 本机私有凭据会进入本机导出的真实版/最终提交 APK，但不会进入 GitHub 或代码包。

### 验收记录

- 阶段 7 验证入口：`CatLife/Runtime/Validate Runtime Assembly`。
- 本阶段要求 Runtime Validator 返回包含 `vivo cloud private APK fallback boundary` 的 PASS。
- 提交前必须执行：
  - 公开范围密钥扫描；
  - `git check-ignore` 验证私有凭据路径；
  - 暂存 diff 密钥扫描；
  - Unity Console 编译错误检查。

## 阶段 8 入口

阶段 8 只处理 Splash 启动页集成：

- 使用用户已给出的开屏素材。
- 不建立森林场景。
- 保持主场景仍是当前小镇首页。
- 20:9 适配，1.5 到 2.5 秒内淡出进入主界面，并支持点击跳过。

## 阶段 8：Splash 启动页集成

完成时间：2026-07-05。

### 本阶段落地内容

- 新增开屏素材：
  - `Assets/Resources/CatLifeSplash/CatLifeSplashLogo.png`
  - 来源为用户提供的 `C:/Users/fujunye/Downloads/ChatGPT Image 2026年7月5日 00_50_15.png`。
- 更新 `CatLifeHomeUiController.cs`：
  - 运行时创建 `CatLifeSplashOverlay`，白底全屏覆盖主 UI。
  - 从 `Resources.Load<Texture2D>("CatLifeSplash/CatLifeSplashLogo")` 加载开屏图并生成 Sprite。
  - 使用 `Image.preserveAspect=true` 适配 20:9 竖屏。
  - 默认 `splashHoldSeconds=1.55f`、`splashFadeSeconds=0.45f`，总时长约 2 秒。
  - 全屏 Button 支持点击跳过。
- 更新 `CatLifeRuntimeAssemblyValidator.cs`：
  - 验证开屏图存在且接近 20:9 竖屏比例。
  - 验证首页控制器包含 splash overlay、淡出和点击跳过入口。
  - 验证 `Assets/Scenes` 下没有新增 forest 场景。

### 保留边界

- 没有建立森林场景。
- 没有替换主场景或改变小镇首页 UI 的默认结构。
- 本阶段未构建 APK；Android 首次启动录屏证据仍属于阶段 9。

### 验收记录

- 阶段 8 验证入口：`CatLife/Runtime/Validate Runtime Assembly`。
- 本阶段要求 Runtime Validator 返回包含 `splash screen integration` 的 PASS。

## 阶段 9 入口

阶段 9 只处理 vivo 云真机 APK 验收和最终运行证据：

- 真实版/最终提交 APK 的硬要求：本机导出时必须包含 `Assets/Resources/CatLifePrivate/vivo_cloud_credentials.json` 中的云真机可用 key，保证上传 vivo 云真机后可以直接尝试真实 API 调用。
- 公开边界不变：GitHub、代码包、公开示例、logcat 归档、截图、录屏字幕和提交记录中不得出现明文 AppKEY、Authorization header 或等价 token。
- Stage 9 不能只以 Editor Play Mode 通过为完成标准；必须有 APK 文件、安装日志、启动录屏、logcat 和密钥边界扫描。
- 如果 BlueLM 端侧 SDK/AAR 尚未具备真机成功证据，PPT 只能写“vivo 云端真实调用演示路径”或“端侧 SDK 初始化失败后 fallback”，不能写“蓝心端侧 SDK 已完成”。
- 如果真实云端 API 因网络、权限、配额或模型名失败，验收仍需保留失败码、fallback 状态和无崩溃证据。

### 阶段 9 验收文件

建议保存到 `06-deliverables/final-submission/evidence/android/`：

- `00-build/build_apk.log`
- `00-build/apk-sha256.txt`
- `00-build/private_config_presence_redacted.txt`
- `01-install/adb_devices.txt`
- `01-install/install.log`
- `02-startup/logcat_startup.txt`
- `02-startup/startup_screenrecord.mp4`
- `03-llm/logcat_vivo_cloud_llm.txt`
- `03-llm/logcat_bluelm_init.txt`
- `03-llm/logcat_bluelm_generate.txt`
- `04-focus/logcat_5min_focus.txt`
- `04-focus/focus_5min_screenrecord.mp4`
- `05-summary/stage9_cloud_phone_result.md`

### 阶段 9 通过条件

- APK 位于 `06-deliverables/final-submission/`，并记录 SHA256。
- 构建前确认本机私有凭据文件存在并被 `.gitignore` 忽略；验证记录只写“存在/可用/脱敏 AppID”，不写明文 AppKEY。
- APK 可安装到 vivo 云真机并启动到开屏页/小镇首页。
- logcat 可判断 `vivo_cloud`、`bluelm_on_device`、`local_template`、失败码或 fallback 状态之一。
- logcat、截图、录屏、提交 diff 和代码包扫描不包含明文 AppKEY。
- 5 分钟流程无 Crash、ANR、猫咪闪烁、猫咪穿模。

## 阶段 9：vivo 云真机 APK 验收自动化（部分落地）

执行时间：2026-07-05。

### 本轮已落地内容

- 新增 `tools/final-submission/collect-stage9-android-evidence.ps1`：
  - 统一创建 `06-deliverables/final-submission/evidence/android/` 下的 `00-build`、`01-install`、`02-startup`、`03-llm`、`04-focus`、`05-summary` 目录。
  - 构建前记录本机私有 vivo 云端配置存在性、`.gitignore` 状态、AppID、AppKEY 是否存在和是否像占位符；不读取或输出明文 AppKEY。
  - APK 存在时记录文件大小和 SHA256。
  - ADB 可用时支持 `adb connect <云真机IP:端口>`、`adb devices`、`adb install -r`、`monkey` 启动、logcat 采集和 `screenrecord` 录屏。
  - 自动发现 Unity 自带 ADB，例如 `D:\UnityEngine\6000.4.9f1\Editor\Data\PlaybackEngines\AndroidPlayer\SDK\platform-tools\adb.exe`，不再要求 adb 必须在 PATH。
  - 自动对 logcat 和命令输出做 `sk-*`、`Authorization: Bearer`、`appKey`、`token` 等模式脱敏。
  - 无 APK、无 ADB、无已连接设备或 dry run 时生成明确的未完成原因，不伪造 Stage9 通过。
- 更新 `tools/final-submission/init-final-evidence.ps1`：
  - 增加 Android 专用证据目录。
  - 增加 `private_config_presence_redacted.txt` 脱敏模板。
- 更新 `tools/final-submission/check-final-submission.ps1`：
  - 增加 `Private APK credential boundary` 检查。
  - 增加 `LLM runtime evidence` 检查。
  - 扩展 Android 安装、logcat、录屏和私有配置脱敏证据识别。
- 更新最终提交 README 和 Android 真机测试记录模板：
  - 增加 Stage9 采证脚本调用方式。
  - 明确真实版 APK 必须包含本机 ignored key，公开材料和日志不得包含明文 key。
- 新增正式 Unity Android 构建入口：
  - `Assets/Editor/CatLifeAndroidBuild.cs`
  - 菜单入口：`CatLife/Build/Build Android APK`
  - batchmode 入口：`CatLife.Editor.CatLifeAndroidBuild.BuildApk`
  - 构建时固定包名 `com.catlife.mvp`、IL2CPP、ARM64、minSdk 28、APK 输出和 ASTC 纹理压缩。
  - 构建前运行 `CatLifeRuntimeAssemblyValidator`，并写入 build settings、runtime validator、APK SHA256 和私有配置脱敏证据。

### 本轮实际证据

- `06-deliverables/final-submission/evidence/android/00-build/private_config_presence_redacted.txt`
  - `Exists: True`
  - `Git ignored: True`
  - `Unity Resources source path: Assets/Resources/CatLifePrivate/vivo_cloud_credentials.json`
  - `Unity Resources source exists: True`
  - `AppID: 2026414599`
  - `AppKEY present: True`
  - `AppKEY placeholder-like: False`
  - `AppKEY value: REDACTED`
- `06-deliverables/final-submission/evidence/android/00-build/apk-sha256.txt`
  - APK：`06-deliverables/final-submission/CatLife_MVP_Android_v0.1.0.apk`
  - Size bytes：`2803906139`
  - SHA256：`97CA85AC82AF3A875B0D61E782B4E5C9506ABB86EE58E3B645CE6A61321A96B1`
- Unity MCP Android build：
  - Result：Succeeded。
  - Duration：约 710.8 秒。
  - Errors：0。
  - Warnings：1629。
  - 说明：构建期间 Console 出现 PVRTC obsolete 相关条目，属于 Android 纹理压缩配置噪声；正式构建入口已固定 `MobileTextureSubtarget.ASTC`，后续重构建应以 ASTC 为准。
- `06-deliverables/final-submission/evidence/android/05-summary/stage9_cloud_phone_result.md`
  - 当前 APK 存在。
  - Unity 自带 ADB 已发现并可执行。
  - 当前 `adb devices` 返回 0 台已连接设备。
  - 当前 Stage9 仍未完成。
- `06-deliverables/final-submission/CatLife_submission_check_20260705.md`
  - `Private APK credential boundary`：PASS。
  - `Secret scan`：PASS。
  - `Android evidence`：MISSING。
  - `LLM runtime evidence`：MISSING。
  - `Recording evidence`：MISSING。

### 保留边界

- 本轮已构建真实版 APK，但 APK 作为本地大二进制交付物被 `.gitignore` 排除，不进入 Git。
- 本轮真实版 APK 的构建前提是本机 ignored 私有 Resources 存在；Unity 构建脚本会在构建前验证 Resources 可加载，PowerShell 采证脚本记录 Resources 源路径存在、AppID、placeholder 判断和 `REDACTED`，不记录明文 AppKEY。
- 本轮没有安装到 vivo 云真机。
- 本轮没有端侧或云端 LLM logcat 成功证据。
- 本轮没有把真实 AppKEY 写进 Git、日志或公开文档。
- Stage9 不能关闭；下一步必须提供 vivo 云真机 ADB endpoint、可用设备序列号或网页实测证据，然后运行采证脚本补齐安装、启动、LLM logcat、5 分钟专注和录屏证据。

## 阶段 10 预备：大模型代码包生成

执行时间：2026-07-05。

### 本轮已落地内容

- 运行 `tools/final-submission/package-llm-code.ps1 -ForFinalSubmission` 生成本地最终代码包：
  - `06-deliverables/final-submission/CatLife_LLM_code_package_v1.zip`
  - SHA256：`A74C26B8304BA5CB239A72456BA5641FE0F7929F51C3C6A6D756E8E02E200F69`
- 生成并提交代码包 manifest：
  - `06-deliverables/final-submission/CatLife_LLM_code_package_manifest.md`
- 更新最终提交检查：
  - `Code package`：PASS。
  - `Secret scan`：PASS，hits=0。
- 修正最终代码包文件名为 ASCII 稳定命名，避免 Windows PowerShell 中文脚本编码导致 zip 文件名乱码。

### 保留边界

- 代码包 zip 是本地最终交付物，按 `.gitignore` 不进入 Git；manifest、README、检查结果和脱敏证据进入 Git。
- 代码包不包含真实 AppKEY；真实版 APK 必须包含本机 ignored 私有 key 用于云真机录制，但 GitHub、代码包、日志、截图、PPT、海报、录屏字幕和公开文档不得包含明文密钥。
- 阶段 10 仍未关闭：视频、云真机安装/logcat/录屏证据仍缺；阶段 9 未通过前不能把最终提交标记为完成。

## 阶段 10 预备：PPT 文件落地

执行时间：2026-07-05。

### 本轮已落地内容

- 将用户提供的 CatLife PPT 复制为本地最终提交 PPT：
  - `06-deliverables/final-submission/CatLife_作品介绍PPT_v1.pptx`
  - Size bytes：`49915319`
  - SHA256：`603D667D706FAAE28EB02BE7E7D9A165A862E4F55835019B8B09603ADF969EE1`
- 新增 PPT manifest：
  - `06-deliverables/final-submission/CatLife_PPT_manifest.md`
- 文件结构验证：
  - PPTX package inspection：PASS。
  - Slide count：23。
  - Media count：44。
  - LibreOffice PDF export：PASS。
  - Exported PDF page count estimate：23。
- 更新最终提交检查：
  - `PPT`：PASS。

### 保留边界

- PPT 是本地最终交付二进制，按 `.gitignore` 不进入 Git；manifest、README、检查结果和人工审查笔记进入 Git。
- PPT 内容仍需人工核对当前 Unity/APK 截图、森林场景规则和大模型/云真机证据表述。
- 阶段 10 仍未关闭：视频、云真机安装/logcat/录屏证据仍缺；阶段 9 未通过前不能把最终提交标记为完成。

## 阶段 10 预备：海报文件落地

执行时间：2026-07-05。

### 本轮已落地内容

- 生成本地最终提交海报：
  - `06-deliverables/final-submission/CatLife_作品海报_v1.png`
  - Size bytes：`5209385`
  - SHA256：`481E7B8EFA95F193121923DF8450FFE89BB9098B0D537632B359A29C96D6F1DD`
  - Dimensions：`4134 x 8858`
  - DPI metadata：约 `150 x 150`
  - 规格口径：竖版 `70cm x 150cm`，PNG，超过 2MB。
- 新增海报 manifest：
  - `06-deliverables/final-submission/CatLife_poster_manifest.md`
- 视觉检查：
  - 已打开 PNG。
  - 已修复初版海报底部黑色矩形问题。
  - 当前图面包含作品名、标语、小镇猫咪主视觉、行为识别/猫咪反馈/隐私友好卖点和可运行 APK / 大模型陪伴口径。

### 私有 key 与云真机口径更新

- 真实版/最终提交 APK 是云真机录制用产物，必须包含本机 ignored 私有 Resources 中的云真机可用 key，保证上传云真机后无需二次配置即可尝试 vivo 云端真实 API。
- 该 key 只能存在于本机私有配置和由本机导出的 APK 中；GitHub、代码包、日志、截图、PPT、海报、视频字幕和公开文档不得出现明文 AppKEY。
- 当前采证和自检只记录 `AppID: 2026414599`、私有配置存在性、`.gitignore` 状态、AppKEY present/placeholder-like 和 `REDACTED`。

### 保留边界

- 海报 PNG 是本地最终交付二进制，按 `.gitignore` 不进入 Git；manifest、README、检查结果和人工审查笔记进入 Git。
- 最终平台上传前仍需人工在平台预览中检查小字号可读性。
- 阶段 10 仍未关闭：视频、云真机安装/logcat/录屏证据仍缺；阶段 9 未通过前不能把最终提交标记为完成。
