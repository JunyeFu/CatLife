# CatLife 开发控制总则

版本：1.0
生效日期：2026-09-03
状态：`ACTIVE`

## 1. 文档权威与目标

本文件是 CatLife 从 MVP 到比赛可提交版本的开发控制入口。发生冲突时，按以下顺序执行：

1. 用户当轮明确指令；
2. 本文件；
3. [L1 缺口与补齐顺序](08-handoff-docs/planning/CatLife_MVP到可提交版_L1缺口与补齐顺序_20260903.md)；
4. [阶段开发方案](08-handoff-docs/planning/CatLife_MVP到可提交版_阶段开发方案_20260903.md)；
5. [验收方案](08-handoff-docs/planning/CatLife_MVP到可提交版_验收方案_20260903.md)；
6. QA-01～QA-14 已确认视觉和产品决策；
7. 旧 DEV-GUIDE、旧 PPT 缺陷表和历史计划。

当前 `0.3.0 (3)` APK 只作为 MVP 基线，不得称为可提交候选版。项目目标是恢复猫咪自主行为、行为识别、已确认前端、可感知 MiMo 闭环和原 PPT 的提交级核心功能，同时保持完整空岛、小镇、包体与离线核心流程。

## 2. 工作范围

本轮产品范围包括：

- Unity 移动运行架构与唯一会话状态源；
- 猫咪移动、动作、兴趣点和场景互动；
- 本 App 内行为特征、状态谱与自动进入专注；
- QA-01～QA-12 确认的 uGUI 前端；
- MiMo 会后单次请求、可见结果和本地猫动作执行；
- 首次领养/命名、轻锁定、成长、记录、设置和地标入口；
- 模拟器、真机、构建和比赛材料证据。

本阶段不实现商城、会员、订阅、社交排行、签到、抽奖、校园后台或森林场景。若比赛材料继续把森林或场景切换描述为已实现能力，必须另立范围变更，不得用概念图代替运行功能。

## 3. 三层任务控制

所有开发项采用三级编号：

| 层级 | 含义 | 示例 | 完成条件 |
|---|---|---|---|
| L1 | 可独立验收的能力阶段 | `L1-02 猫咪动作与移动` | 对应硬门通过 |
| M | 单一模块或交付面 | `M2.1 导航与兴趣点` | 模块测试和集成证据齐全 |
| S | 可直接执行的小任务 | `S2.1.1 烘焙移动场景 NavMesh` | 有明确文件、验证命令和结果 |

每个任务必须记录：任务 ID、目标、输入、允许修改的文件、禁止修改项、验收用例、证据路径、状态和剩余问题。没有验收项的任务不得进入 `IN_PROGRESS`。

## 4. 状态与在制品限制

任务状态固定为：

```text
BACKLOG -> READY -> IN_PROGRESS -> VERIFYING -> PASS
                                  -> FAILED
                                  -> BLOCKED
```

- 同时只允许一个 L1 处于 `IN_PROGRESS` 或 `VERIFYING`。
- 当前 L1 内同时只允许一个会改变同一场景/Prefab 的 M 任务写入。
- `FAILED` 必须记录复现步骤和责任层；修复后重新走完整验收。
- `BLOCKED` 只用于缺少外部权限、设备或用户决策，不能用于普通开发困难。
- 后续 L1 可以做只读准备，但前一硬门未通过时不得正式合并。

## 5. 架构控制

### 5.1 唯一真源

- `CatLifeSessionController` 是会话阶段、截止时间、完成/中断和奖励的唯一真源。
- `CatLifeMobileApp` 只做页面与模块协调，不再承载新的算法规则。
- Recognition 输出聚合快照，不直接切换页面、播放动画或写奖励。
- Cat Behavior 消费会话阶段、识别快照和允许列表内的 MiMo 建议，不修改会话时钟。
- UI 只显示状态并发出用户意图，不复制业务状态。
- MiMo 不得控制计时、奖励、持久化或任意 Animator 状态。

### 5.2 事件主链

```text
UI/Android 生命周期事件
  -> Recognition 聚合特征
  -> Session 状态决策
  -> Cat Behavior 动作/移动
  -> Presenter 与 UI 呈现
  -> 完成后 MiMo 单次建议
  -> 本地允许列表映射为猫咪反应
```

同一事件只能有一个所有者。禁止建立第二套会话状态机、第二套计时器或绕过本地允许列表直接执行模型文本。

## 6. 美术、UI 与资产控制

- 原始 Blender、GLB、FBX 和高分辨率图片只读保留，不就地覆盖。
- Unity 运行资产只进入 `Assets/MobileRuntime/Art`，通过稳定资产 ID 引用。
- 完整空岛、小镇背景、关键地标和猫咪主色关系是独立视觉硬门；预算通过不能抵消视觉错误。
- UI 以 QA-01～QA-12 为冻结目标；AI 生图是设计意图，真实模拟器截图才是实现证据。
- 运行 UI 不得继续以 Unity 内置矩形 Sprite 作为最终提交视觉。
- 近景摄像机保持固定分镜；编辑器全岛审计机位单独保留。

## 7. 行为识别、隐私与 MiMo 控制

- 默认只采集本 App 内点击、滑动、停顿、页面切换、猫互动和前后台时间。
- 不采集屏幕内容、输入文本、原始触点序列、应用包名或跨 App 触控。
- 上传 MiMo 的内容只允许是会话聚合值；首次启用必须取得同意。
- 每个完成会话最多请求一次；失败不自动重试，本地洞察继续可用。
- 用户必须看见请求中、结果来源、建议内容和最终执行的猫咪反应。
- API 凭据只进入本地忽略配置和候选 APK，不进入 Git、日志、截图、PPT、视频字幕或代码包。

## 8. 证据等级

| 等级 | 证据 | 能证明 | 不能证明 |
|---|---|---|---|
| E0 | 代码、Prefab、清单静态检查 | 文件与契约存在 | 运行可用 |
| E1 | EditMode/单元测试 | 纯逻辑正确 | 场景装配与画面正确 |
| E2 | PlayMode/Editor 截图 | Unity 集成可运行 | APK 行为正确 |
| E3 | Android 模拟器截图、录屏、logcat | 当前 APK 在模拟器可运行 | 真机性能与兼容性 |
| E4 | 真实 MiMo 请求日志与可见结果 | 云端链路真实成功 | 真机整体通过 |
| E5 | 指定真机记录 | 设备兼容、恢复和性能 | 比赛材料已一致 |
| E6 | 同一候选包的提交材料审计 | 可提交包闭环 | 平台已成功收件 |

硬门要求的证据等级不得由更低等级替代。旧日志和旧截图只能证明其对应旧构建。

## 9. 硬门与发布纪律

硬门固定为 `G1 -> G2 -> G3 -> G4 -> G5 -> G6 -> G7`。验收结论只有 `PASS`、`FAIL`、`BLOCKED`；不得使用“基本通过”或“数值通过但视觉待定”。

- G1～G6 通过前不创建提交候选版本号。
- 新候选不得覆盖 `0.3.0` MVP APK。
- Release APK/AAB 必须不超过 120 MiB，并能离线完成核心流程。
- 不设置 MD5、SHA 或其他文件哈希门槛；资产追踪使用路径、版本、资产 ID、构建身份和实际报告。
- 只有同一候选包通过的能力才能写入 PPT、海报、视频和答辩稿。

## 10. Git 与变更纪律

- 开工和收口均检查 `git status --short --branch`。
- 既有脏工作树默认属于用户；不得清理、覆盖或混入任务提交。
- 只暂存任务明确修改的路径；私有配置、APK、截图、日志和大型派生二进制不提交。
- 每个通过硬门的 L1 使用一个可回退提交；提交信息包含能力域，不使用“misc/fix all”。
- 测试失败或硬门失败时不推送“完成”提交。

## 11. 完成定义

一个 L1 只有同时满足以下条件才可标记 `PASS`：

1. 该阶段的 M/S 任务全部关闭；
2. 相关 EditMode、PlayMode、Editor、模拟器或真机证据达到规定等级；
3. 视觉、功能、数据和隐私验收没有未处理阻断项；
4. 验收报告写明构建身份、设备、结果和证据路径；
5. Git 状态已复核，只提交本阶段文件；
6. 下阶段入口条件明确。

G1～G3 已通过。当前唯一允许启动的正式阶段是 `L1-04 已确认前端画面`；G4 通过前不启动 MiMo 可感知闭环的正式合并。
